local Oldfunction Oldfunction=hookfunction(getrenv()._G.FPS.EquipItem,function(...)	local Args = {...} 	local p2 = Args[1]	_G.WEAPONID=p2.id	return Oldfunction(...) end) old1=nil old2=nil old3=nil old1=hookfunction(getrenv()._G.Camera.Recoil,function(...) if _G.NoRecoil then 	return nil 	else 	return old1(...) 	end end) old2=hookfunction(getrenv()._G.Camera.setSwayVelocity,function(...) 	if _G.NoRecoil then 		return nil 	else 		return old2(...) 	end end) old3=hookfunction(getrenv()._G.Camera.setSwayMagnitude,function(...)	if _G.NoRecoil then	return nil	else 	return old3(...)	end end)
while task.wait(3) do
		LPH_JIT_ULTRA(function()
			for i,v in pairs(game.Workspace:GetChildren()) do
				if v:IsA('Model') and v:FindFirstChild('Head') and v:FindFirstChild('HumanoidRootPart') and not IsSleeping(v.Head) then
					v.Parent = game.Workspace.Players
				end
			end	
		end)()
	end
